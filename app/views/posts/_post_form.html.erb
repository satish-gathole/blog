<%= form_for @post  do |f| %>
    <div class="field">
      <%= f.text_area :content, :placeholder => "Compose new post...", :value => @default_post_text %>
    </div>
    <%= f.submit "Post", :class => "btn btn-large btn-primary" %>  </br>
<% end %>